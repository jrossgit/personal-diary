
## Quickstart

To run things locally on the command line, enter the virtual environment and instsall:

```bash
eval $(poetry env activate)
poetry install
```

To run a local dev instance, run

```bash
fastapi dev diary_api/main.py
```
